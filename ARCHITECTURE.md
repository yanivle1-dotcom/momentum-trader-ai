# 🏗️ ארכיטקטורה של המערכת

## סקירה כללית

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                            │
│                     (Chrome, Firefox, etc.)                     │
│                         localhost:5000                          │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTP/HTTPS
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                        FLASK WEB SERVER                         │
│                          (Port 5000)                            │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Routes:                                                  │ │
│  │  GET  /              → Dashboard HTML                    │ │
│  │  GET  /api/scan      → Scan stocks                       │ │
│  │  POST /api/analyze   → Analyze specific stock            │ │
│  │  GET  /api/chart     → Get chart data                    │ │
│  │  GET  /api/agents    → List available AI agents          │ │
│  │  GET  /api/exchange-rate → Get USD/ILS rate              │ │
│  └───────────────────────────────────────────────────────────┘ │
└───────────────────────────┬─────────────────────────────────────┘
                            │
          ┌─────────────────┼─────────────────┐
          │                 │                 │
┌─────────▼─────┐  ┌────────▼────────┐  ┌────▼─────────┐
│ AI AGENTS     │  │ MARKET DATA     │  │ ANALYSIS     │
│ Layer         │  │ Layer           │  │ Layer        │
└─────┬─────────┘  └────────┬────────┘  └────┬─────────┘
      │                     │                 │
      │                     │                 │
```

---

## שכבת AI Agents

```
┌──────────────────────────────────────────────────┐
│              BaseAgent (Abstract)                │
│  - build_analysis_prompt()                      │
│  - validate_response()                          │
│  - analyze_stock() [abstract]                   │
└────────────────┬─────────────────────────────────┘
                 │
    ┌────────────┼────────────┐
    │            │            │
┌───▼───────┐ ┌──▼─────────┐ ┌▼──────────┐
│ OpenAI    │ │  Gemini    │ │Perplexity │
│ Agent     │ │  Agent     │ │  Agent    │
├───────────┤ ├────────────┤ ├───────────┤
│ Model:    │ │ Model:     │ │ Model:    │
│ GPT-4     │ │ gemini-pro │ │ sonar-lg  │
│           │ │            │ │ +search   │
│ Features: │ │ Features:  │ │ Features: │
│ - Fast    │ │ - Strong   │ │ - Real    │
│ - JSON    │ │ - Hebrew   │ │   time    │
│   format  │ │   support  │ │ - Citations│
└───────────┘ └────────────┘ └───────────┘
     │              │              │
     └──────────────┼──────────────┘
                    │
            ┌───────▼───────┐
            │ Analysis JSON │
            │ {             │
            │  catalyst,    │
            │  setup_type,  │
            │  analysis,    │
            │  warnings     │
            │ }             │
            └───────────────┘
```

---

## שכבת Market Data

```
┌─────────────────────────────────────────────────┐
│          MarketDataFetcher                      │
├─────────────────────────────────────────────────┤
│ Methods:                                        │
│                                                 │
│ get_stock_data(symbol, period)                 │
│   └─→ Returns: DataFrame with OHLCV            │
│                                                 │
│ calculate_indicators(df)                       │
│   └─→ Adds: VWAP, EMA9, EMA20, RSI             │
│                                                 │
│ get_current_data(symbol)                       │
│   └─→ Returns: Dict with current metrics       │
│                                                 │
│ scan_for_momentum(symbols, criteria)           │
│   └─→ Returns: List of stocks passing filters  │
└────────────────┬────────────────────────────────┘
                 │
                 │ Uses
                 │
┌────────────────▼────────────────────────────────┐
│           yfinance Library                      │
├─────────────────────────────────────────────────┤
│ ticker.history()    → Historical OHLCV          │
│ ticker.info         → Stock information         │
│ ticker.preMarket    → Pre-market data           │
└────────────────┬────────────────────────────────┘
                 │
                 │ Fetches from
                 │
┌────────────────▼────────────────────────────────┐
│          Yahoo Finance API                      │
│      (Public, No Key Required)                  │
└─────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────┐
│         CurrencyConverter                       │
├─────────────────────────────────────────────────┤
│ get_usd_to_ils_rate()                          │
│   └─→ Returns: float (e.g., 3.60)              │
│                                                 │
│ usd_to_ils(amount)                             │
│   └─→ Converts and returns ILS amount          │
│                                                 │
│ Cache: 1 hour TTL                              │
└────────────────┬────────────────────────────────┘
                 │
                 │ Fetches from
                 │
┌────────────────▼────────────────────────────────┐
│       ExchangeRate-API.com                      │
│      (Free tier: 1,500 req/month)              │
└─────────────────────────────────────────────────┘
```

---

## שכבת Analysis

```
┌──────────────────────────────────────────────────┐
│        RossCameronAnalyzer                       │
├──────────────────────────────────────────────────┤
│ analyze_setup(stock_data, historical_df)        │
│   │                                              │
│   ├─→ _check_gap_and_go()                       │
│   │    └─→ Score: 0-7                           │
│   │                                              │
│   ├─→ _check_red_to_green()                     │
│   │    └─→ Score: 0-7                           │
│   │                                              │
│   ├─→ _check_first_green_day()                  │
│   │    └─→ Score: 0-8                           │
│   │                                              │
│   ├─→ _check_micro_pullback()                   │
│   │    └─→ Score: 0-7                           │
│   │                                              │
│   └─→ _check_bull_flag()                        │
│        └─→ Score: 0-7                           │
│                                                  │
│ Returns best setup with:                        │
│  - setup_type                                   │
│  - criteria_met / criteria_failed               │
│  - entry_point, stop_loss, targets             │
│  - risk_reward ratio                            │
│  - confidence level                             │
└──────────────────────────────────────────────────┘

Setup Scoring Logic:
┌──────────────────────────────────────┐
│ Each criterion adds points:         │
│                                      │
│ ✓ Gap 3%+           → +3 points     │
│ ✓ RVOL 2x+          → +2 points     │
│ ✓ Price > VWAP      → +1 point      │
│ ✓ Price > EMA9      → +1 point      │
│ ✓ Strong volume     → +2 points     │
│                                      │
│ Valid if score ≥ 5                  │
│ High confidence if score ≥ 6        │
└──────────────────────────────────────┘
```

---

## Data Flow - סריקת מניות

```
1. User clicks "סרוק מניות"
   │
   ▼
2. Frontend: GET /api/scan
   │
   ▼
3. Backend: Load stocks from config/stocks.json
   │
   ▼
4. For each stock:
   │
   ├─→ MarketDataFetcher.get_current_data(symbol)
   │   │
   │   ├─→ yfinance.Ticker(symbol)
   │   ├─→ Calculate indicators (VWAP, EMA, RSI)
   │   └─→ Calculate RVOL, Gap
   │
   └─→ Filter by criteria:
       ├─→ RVOL ≥ 2.0
       ├─→ Gap ≥ 3%
       └─→ Volume ≥ 100K
   │
   ▼
5. Sort by RVOL (highest first)
   │
   ▼
6. CurrencyConverter.usd_to_ils() for US stocks
   │
   ▼
7. Return JSON with all passing stocks
   │
   ▼
8. Frontend: Render stock cards
```

---

## Data Flow - ניתוח מניה ספציפית

```
1. User clicks on stock card
   │
   ▼
2. Frontend: POST /api/analyze/<symbol>
   Body: { "agent": "chatgpt" }
   │
   ▼
3. Backend parallel execution:
   │
   ├─────────────────────┬───────────────────┐
   │                     │                   │
   ▼                     ▼                   ▼
MarketData          RossCameron         AI Agent
get_current()       analyze_setup()     analyze_stock()
   │                     │                   │
   │ Returns:            │ Returns:          │ Returns:
   │ - Price             │ - Setup type      │ - Catalyst
   │ - Volume            │ - Entry/Stop      │ - News
   │ - Indicators        │ - Targets         │ - Analysis
   │                     │ - Confidence      │ - Warnings
   │                     │                   │
   └─────────────────────┴───────────────────┘
                         │
                         ▼
            Combine all results into JSON
                         │
                         ▼
            Add currency conversion
                         │
                         ▼
            Return complete analysis
                         │
                         ▼
            Frontend: Show modal with:
            - Stock data
            - Setup analysis
            - AI insights
            - Chart
```

---

## Data Flow - גרף

```
1. User opens analysis modal
   │
   ▼
2. Frontend: GET /api/chart/<symbol>?period=5d
   │
   ▼
3. Backend:
   │
   ├─→ MarketDataFetcher.get_stock_data(symbol, "5d")
   │   └─→ yfinance returns 5-minute candles
   │
   ├─→ MarketDataFetcher.calculate_indicators()
   │   └─→ Adds VWAP, EMA9, EMA20, RSI
   │
   └─→ Convert DataFrame to JSON arrays
   │
   ▼
4. Return:
   {
     timestamps: [...],
     open: [...],
     high: [...],
     low: [...],
     close: [...],
     volume: [...],
     vwap: [...],
     ema_9: [...],
     ema_20: [...],
     rsi: [...]
   }
   │
   ▼
5. Frontend: Plotly.js renders:
   - Candlestick chart
   - VWAP line (orange)
   - EMA9 line (blue)
   - EMA20 line (red)
   - Volume bars (bottom)
```

---

## Configuration Flow

```
┌─────────────────────────────────────┐
│      .env (Environment)             │
├─────────────────────────────────────┤
│ OPENAI_API_KEY=sk-...              │
│ GEMINI_API_KEY=...                 │
│ PERPLEXITY_API_KEY=pplx-...        │
│ EXCHANGE_RATE_API_KEY=...          │
│ FLASK_PORT=5000                    │
│ DEBUG=True                         │
└──────────────┬──────────────────────┘
               │ Loaded by
               │ python-dotenv
               ▼
┌─────────────────────────────────────┐
│    Flask app initialization         │
│                                     │
│ 1. Load .env                        │
│ 2. Initialize agents with keys     │
│ 3. Check which agents available    │
│ 4. Start server on port            │
└─────────────────────────────────────┘


┌─────────────────────────────────────┐
│  config/stocks.json                 │
├─────────────────────────────────────┤
│ {                                   │
│   "israeli_stocks": [...],          │
│   "us_stocks": [...],               │
│   "momentum_criteria": {            │
│     "min_rvol": 2.0,               │
│     "min_gap_percent": 3.0,        │
│     "min_volume": 100000           │
│   }                                 │
│ }                                   │
└──────────────┬──────────────────────┘
               │ Loaded on startup
               │ and on /api/scan
               ▼
┌─────────────────────────────────────┐
│   Used for:                         │
│   - Stock scanning                  │
│   - Filtering criteria              │
│   - Dynamic watchlist               │
└─────────────────────────────────────┘
```

---

## Error Handling

```
┌─────────────────────────────────────┐
│         Error Hierarchy             │
└─────────────────────────────────────┘

User Action
   │
   ▼
Try:
   │
   ├─→ API call fails?
   │   └─→ Return { success: false, error: msg }
   │
   ├─→ Stock data unavailable?
   │   └─→ Return { data_available: false }
   │
   ├─→ AI agent error?
   │   └─→ Return analysis with error field
   │
   └─→ Network timeout?
       └─→ Frontend shows error message

Frontend always checks:
   if (data.success) {
      // Render results
   } else {
      // Show error to user
   }
```

---

## Security Architecture

```
┌──────────────────────────────────────┐
│         Security Layers              │
└──────────────────────────────────────┘

1. Environment Variables
   ├─→ .env file (NOT in git)
   ├─→ .gitignore prevents commits
   └─→ No hardcoded secrets

2. API Key Management
   ├─→ Keys loaded at runtime
   ├─→ Not exposed to frontend
   └─→ Server-side only

3. CORS
   ├─→ flask-cors enabled
   └─→ Controlled origins

4. Input Validation
   ├─→ Symbol format checking
   ├─→ Period validation
   └─→ Agent name whitelist

5. Rate Limiting (future)
   └─→ Prevent abuse
```

---

## Scalability Considerations

### Current Architecture
- **Single server** (Flask dev server)
- **Synchronous requests**
- **No database** (stateless)
- **No caching** (except currency)

### For Production Scale:

```
┌────────────────────────────────────┐
│       Load Balancer                │
└─────────────┬──────────────────────┘
              │
    ┌─────────┼─────────┐
    │         │         │
┌───▼───┐ ┌──▼───┐ ┌───▼───┐
│Flask 1│ │Flask 2│ │Flask 3│
└───┬───┘ └──┬───┘ └───┬───┘
    │        │         │
    └────────┼─────────┘
             │
    ┌────────▼────────┐
    │   Redis Cache   │
    │  - Stock data   │
    │  - AI responses │
    └─────────────────┘
             │
    ┌────────▼────────┐
    │   PostgreSQL    │
    │  - Trade logs   │
    │  - User prefs   │
    └─────────────────┘
```

---

## Performance Optimization Points

### Current:
1. **No database** → Fast, stateless
2. **Direct API calls** → Real-time data
3. **Client-side rendering** → Reduce server load

### Potential Improvements:
1. **Redis cache** for stock data (5-min TTL)
2. **Async/await** for parallel API calls
3. **WebSocket** for real-time updates
4. **CDN** for static files
5. **Lazy loading** of charts

---

## Technology Stack Summary

```
┌─────────────────────────────────────────────┐
│              Frontend                       │
├─────────────────────────────────────────────┤
│ HTML5, CSS3 (Flexbox, Grid)                │
│ Vanilla JavaScript (ES6+)                  │
│ Plotly.js (Charts)                         │
│ Fetch API (AJAX)                           │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│              Backend                        │
├─────────────────────────────────────────────┤
│ Python 3.8+                                │
│ Flask 3.0 (Web framework)                  │
│ flask-cors (CORS support)                  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│           Data & Analysis                   │
├─────────────────────────────────────────────┤
│ yfinance (Market data)                     │
│ pandas (Data manipulation)                 │
│ pandas-ta (Technical indicators)           │
│ numpy (Numerical operations)               │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│              AI Services                    │
├─────────────────────────────────────────────┤
│ OpenAI API (GPT-4)                         │
│ Google Gemini API                          │
│ Perplexity API                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│            External APIs                    │
├─────────────────────────────────────────────┤
│ Yahoo Finance (Free, no key)               │
│ ExchangeRate-API (Free tier)               │
└─────────────────────────────────────────────┘
```

---

## Deployment Architecture

### Development (Current)
```
┌──────────────────┐
│  Local Machine   │
│  ┌────────────┐  │
│  │ Python 3   │  │
│  │ venv       │  │
│  │ Flask dev  │  │
│  │ Port 5000  │  │
│  └────────────┘  │
└──────────────────┘
```

### Production (Future)
```
┌────────────────────────────────┐
│         Cloud Provider         │
│  (AWS/GCP/Azure/Heroku)        │
│                                │
│  ┌──────────────────────────┐  │
│  │  Gunicorn + Flask        │  │
│  │  (WSGI Server)           │  │
│  └────────┬─────────────────┘  │
│           │                    │
│  ┌────────▼─────────────────┐  │
│  │  Nginx (Reverse Proxy)   │  │
│  │  SSL/TLS                 │  │
│  └──────────────────────────┘  │
└────────────────────────────────┘
```

---

נבנה עם ❤️ לקהילת הטריידרים 🚀
